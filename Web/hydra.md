a fast, parallelized network logon cracker -> hydra
    syntax -> hydra -l <username> -P <password_file> -s <port> <target_host> http-post-form "<url_path>:<form_fields>:<failure_string>" -o <output_file>

